import React from 'react';
import "./style.css";

export default function Card() {
    return (
        <div className='Container-Card'>
            <h1>ISSO É UM COMPONENTE</h1>
            <p>Componentes são elementos visuais exibidos na tela para o usuario, em uma página web um botão é um componente que dentro dele existem outros componentes, um footer é um componente.  </p>
        </div>
    );
}